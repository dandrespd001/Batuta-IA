[package]
name = "batuta-lease"
description = "Admisión por leases: por modelo y por repositorio. Caducan por evidencia, nunca por antigüedad."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true, features = ["std"] }
serde_json = { workspace = true }

[lints]
workspace = true
